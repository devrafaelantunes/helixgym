<h1>Log in</h1>

<%= form_for @conn, Routes.employee_session_path(@conn, :create), [as: :employee], fn f -> %>
  <%= if @error_message do %>
    <div class="alert alert-danger">
      <p><%= @error_message %></p>
    </div>
  <% end %>

  <%= label f, :document %>
  <%= text_input f, :document, required: true %>

  <%= label f, :password %>
  <%= password_input f, :password, required: true %>

  <%= label f, :remember_me, "Keep me logged in for 60 days" %>
  <%= checkbox f, :remember_me %>

  <div>
    <%= submit "Log in" %>
  </div>
<% end %>

<p>
  <%= link "Register", to: Routes.employee_registration_path(@conn, :new) %> |
  <%= link "Forgot your password?", to: Routes.employee_reset_password_path(@conn, :new) %>
</p>
